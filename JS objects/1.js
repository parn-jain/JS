// Sample object
var student = {
  name: "David Rayy",
  sclass: "VI",
  rollno: 12
};

// Get the list of property names (keys)
var propertyNames = Object.keys(student);
// Object.keys(student) returns an array of the property names: ["name", "sclass", "rollno"]

// Output the property names as a comma-separated string
console.log(propertyNames);
